apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: k8s-backend  
  labels:
    app: mongo
spec:
  clusterIP: None   #Headless Service
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
